# Unraid Templates for watchparty

XML templates for Unraid Community Applications.

## Install
Copy `my-watchparty.xml` to `/boot/config/plugins/dockerMan/templates-user/` on Unraid,
then: Docker tab → Add Container → select from template list.
